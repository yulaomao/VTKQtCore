# 方案设计：多软件启动装配改造

同一代码基座下承载多个独立软件，推荐采用“公共核心库 + 可选模块库 + 独立产品启动 target”的单一方案。这样既能复用现有 LogicRuntime、BaseSoftwareInitializer、CommunicationHub、UI 协调体系，又能满足你当前已确认的约束：多个 EXE、独立名称图标版本安装包、只按软件启用需要的模块，并尽量不把未启用模块的资源和依赖带入产物。

## 需求摘要

- 目标是在一个大项目中同时维护多个软件项目。
- 不同软件必须产出多个独立 EXE，而不是单 EXE 运行时切换。
- 软件之间的主要差异是模块装配差异，核心框架尽量共用。
- 新增软件允许增加独立 target 和独立入口，不要求完全零代码接入。
- 未启用模块不应只隐藏 UI，而应尽量不进入对应软件的最终构建产物。
- 每个软件需要独立名称、图标、版本信息和安装包。
- 非目标是重写现有运行时、重写所有模块接口、或把项目改造成插件热加载系统。

## 调研结论

- 当前启动链路已经具备基础多变体能力：src/main.cpp 负责读取 software profile、选择 softwareType，并通过 SoftwareInitializerFactory 创建具体装配器。
- 当前产品差异主要承载在 BaseSoftwareInitializer 扩展点中，包括模块启用列表、初始模块、模块顺序、逻辑注册、UI 装配、产品根界面和通信源注册。
- 当前仓库已经存在不同 initializer 的实践，说明“同核心、不同装配”在运行时层面是成立的。
- 当前真正的瓶颈不在运行时，而在工程组织：根级 CMakeLists.txt 直接把 src 下所有 h/cpp/ui/qrc 递归打进单个 VTKQtCore 可执行程序，这会导致所有模块源码和资源天然进入同一 target。
- 在现有结构下，即使 profile 不启用某模块，也不能保证该模块的资源、依赖和代码不会出现在产物里。
- 因此，如果目标是“多个 EXE + 产物级隔离”，仅靠新增若干 initializer 不够，必须同步改造构建分层和模块编译边界。

## 推荐方案

- 采用三层结构：公共核心层、模块能力层、产品启动层。
- 保留当前 BaseSoftwareInitializer 作为装配骨架，但不再让每个产品 initializer 手写一整套模块注册逻辑，而是把模块装配收敛到统一的模块描述与安装接口。
- 每个软件对应一个独立 executable target；每个 target 只链接公共核心库和所需模块库，再绑定自己的品牌资源与版本信息。
- 运行时仍可保留 software profile 作为该产品内部的调试或环境配置手段，但发布态不再依赖单 EXE 多 softwareType 切换。

推荐的目标结构如下：

- 公共核心库：承载 contracts、logic/runtime、communication、display、shell、公共 UI 协调、BaseSoftwareInitializer、通用启动工具。
- 模块库：每个业务模块独立为一个静态库或对象库，包含该模块的 logic、UI、局部资源和该模块的装配入口。
- 产品 target：每个软件一个独立 executable，包含产品 main、产品描述、图标版本信息、默认 profile、安装脚本元数据。

推荐新增两个核心抽象：

- ProductDefinition：描述一个软件产品的静态身份和默认装配规则，包括 productId、显示名、默认模块列表、模块顺序、初始模块、主题、默认通信配置、产品资源入口。
- ModulePack：描述一个模块如何被装配，包括 moduleId、逻辑注册函数、UI 注册函数、可选全局控件注册、可选通信源注册、该模块资源归属。

装配流程调整为：

- 产品 main 只负责加载当前产品自己的 ProductDefinition 和可选外部 profile。
- 通用 ProductBootstrap 根据 ProductDefinition 创建运行时对象和 ProductSoftwareInitializer。
- ProductSoftwareInitializer 不再硬编码每个模块类名，而是读取产品声明的模块列表，并从 ModulePackRegistry 中逐个安装。
- 只有被当前产品 target 链接进来的 ModulePack 才能参与注册，从而实现模块级编译隔离。

这个方向适合当前约束，原因如下：

- 它复用了已有的初始化生命周期和 UI/logic 协调模型，避免框架层重写。
- 它把“多软件”问题拆成了两个可控维度：构建产物隔离和运行时模块装配，不再混在一个单体 target 里处理。
- 它满足独立 EXE/图标/版本/安装包要求，同时保留公共核心的统一维护成本。
- 它比真正插件化更简单，也更符合当前 Qt/CMake/VTK 工程现实。

关键取舍如下：

- 不推荐继续沿用“一个单体可执行程序 + 多个 initializer 子类 + 多份 profile”作为最终方案，因为这无法满足产物级隔离。
- 不推荐一步走到动态插件热插拔，因为当前需求只是多软件装配，不需要运行时下载和热替换模块。
- 推荐把“产品差异”放在 target 和 ProductDefinition 层，把“模块能力”放在 ModulePack 层，把“装配时序”保留在 BaseSoftwareInitializer 层。

## 实施阶段

1. 先拆构建层，把当前单个 VTKQtCore 可执行程序改为一个公共核心库 vtkqtcore_core，并停止用根级递归 GLOB 直接把整个 src 打进单一 EXE。
2. 按模块建立编译边界，把 src/modules 下每个业务模块拆成独立 module_<id> 静态库或对象库，并为每个模块补齐统一的装配入口文件。
3. 引入 ProductDefinition、ModulePack、ModulePackRegistry 和 ProductBootstrap，让产品装配从“产品 initializer 手写注册代码”变成“根据产品声明遍历模块包安装”。
4. 为每个软件建立独立产品目录，例如 src/products/<product_id>/，放置该产品的 main、默认配置、品牌资源、版本信息和安装打包元数据。
5. 在 CMake 中增加 add_product_target(product_id ...) 一类的公共函数，用于声明产品名称、链接模块列表、图标版本信息、默认 profile 和安装输出目录。
6. 将现有 DefaultSoftwareInitializer 和精简 initializer 迁移成“产品定义 + 模块列表”的形式，只保留确有必要的产品级定制逻辑。
7. 调整启动策略：发布态每个产品 EXE 固定自己的 ProductDefinition；开发态可继续支持 --software-profile 覆盖该产品内部配置，但不再跨产品切换 softwareType。
8. 为每个产品建立独立打包与回归验证流程，验证模块显示、模块逻辑、资源装配和依赖裁剪是否正确。

## 相关文件 / 模块

- d:/code/C++/VTKQtCore/CMakeLists.txt — 当前是单 target + 全量递归 GLOB，必须先改造为“核心库 + 模块库 + 产品 target”。
- d:/code/C++/VTKQtCore/src/main.cpp — 当前唯一入口，后续应下沉为共享启动工具或被产品 main 复用。
- d:/code/C++/VTKQtCore/src/app/software/BaseSoftwareInitializer.h — 保留为装配骨架，继续承担统一初始化时序。
- d:/code/C++/VTKQtCore/src/app/software/SoftwareInitializerFactory.h — 可保留给开发态或兼容层使用，但发布态不应再承担跨产品选择职责。
- d:/code/C++/VTKQtCore/src/app/software/ConcreteSoftwareInitializers/DefaultSoftwareInitializer.cpp — 现有产品装配样板，后续应迁移为基于 ProductDefinition/ModulePack 的声明式装配。
- d:/code/C++/VTKQtCore/src/app/software/ModuleUiAssemblers.cpp — 适合作为模块 UI 装配入口的收敛点，但需要按模块拆分归属。
- d:/code/C++/VTKQtCore/src/modules — 业务模块源码，应成为模块库拆分的主战场。
- d:/code/C++/VTKQtCore/项目对标文档/01-启动与装配规范.md — 需要在实现后同步更新，反映“多产品 target + 产品定义”的新启动模型。
- d:/code/C++/VTKQtCore/项目对标文档/03-运行时与生命周期规范.md — 需要补充产品启动层与模块安装层在生命周期中的位置。

建议新增的结构如下：

- d:/code/C++/VTKQtCore/src/app/bootstrap/ProductDefinition.h
- d:/code/C++/VTKQtCore/src/app/bootstrap/ProductBootstrap.h
- d:/code/C++/VTKQtCore/src/app/modules/ModulePack.h
- d:/code/C++/VTKQtCore/src/app/modules/ModulePackRegistry.h
- d:/code/C++/VTKQtCore/src/products/<product_id>/main.cpp
- d:/code/C++/VTKQtCore/src/products/<product_id>/product.json
- d:/code/C++/VTKQtCore/src/products/<product_id>/resources/

## 验收标准

- 工程中可以同时生成多个独立产品 EXE，每个 EXE 有独立名称、图标、版本信息和安装产物。
- 每个产品 EXE 只包含声明链接的模块库，不启用的模块不会被注册，也不会出现在产品导航和启动流程中。
- 未链接模块的资源和依赖不会被误带入对应产品产物目录。
- 公共核心逻辑仅维护一份，多个产品共享同一套运行时、通信、显示和 UI 协调代码。
- 新增一个软件时，增量工作被约束在“新增产品目录 + 指定模块列表 + 品牌资源 + CMake 声明”，不需要复制整套主程序框架。
- 现有默认产品和精简产品都能在新结构下正常运行，行为与迁移前保持一致。

## 验证方式

1. 配置并构建全部产品 target，确认 CMake 能分别生成多个 EXE，而不是只生成单一 VTKQtCore。
2. 分别启动每个产品，检查初始模块、模块顺序、导航项和主界面是否符合各自 ProductDefinition。
3. 对每个产品执行最小功能回归，确认 LogicRuntime、CommunicationHub、SceneGraph、全局控件和模块通知链路不受拆分影响。
4. 检查每个产品输出目录中的二进制、Qt 部署结果、资源文件和依赖库，确认未启用模块的专属资源和依赖未进入该产物。
5. 执行安装包验证，确认不同产品的安装名称、图标、版本信息和安装目录互不冲突。

## 风险与缓解

- 当前使用递归 GLOB 收源，拆分后容易遗漏源文件或资源归属。缓解方式是改为分层 CMake 和显式 target 归属，逐步迁移而不是一次性平移所有文件。
- 现有模块之间可能存在隐式头文件依赖或静态注册副作用。缓解方式是先按模块梳理编译依赖，优先消除跨模块直接 include 和隐式初始化。
- Qt 资源文件若继续共用全局 qrc，可能导致多个产品 target 仍然携带无关资源。缓解方式是把品牌资源和模块资源按 target 拆 qrc。
- Windows 打包链当前绑定单目标，拆成多产品后 windeployqt 和安装脚本需要同步参数化。缓解方式是把部署和打包逻辑封装成按 target 复用的 CMake 函数。
- 迁移期间同时维护旧入口和新入口会增加短期复杂度。缓解方式是先保留旧 VTKQtCore 作为兼容 target，待两个产品 target 稳定后再下线旧入口。

## 决策与假设

- 已确认采用多个 EXE 共用同一套核心代码，而不是单 EXE 多 softwareType 交付。
- 已确认产品差异当前主要是模块装配差异，不把整体 UI 壳层大改作为本次前提。
- 已确认新增软件允许拥有独立 target 和独立入口。
- 已确认未启用模块应尽量做到编译和产物层面的隔离，而不是只做运行时隐藏。
- 已确认每个产品需要独立名称、图标、版本信息和安装包。
- 假设当前绝大多数模块仍可共享同一套 contracts、runtime 和 communication 机制，不需要按产品分叉协议。

## 待确认项

- 无。
