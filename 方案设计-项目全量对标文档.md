# 方案设计：项目全量对标文档

TL;DR：目标不是补一篇介绍，而是建设一套可直接支撑跨平台、跨语言复刻的项目级规范文档。文档必须以当前仓库实际代码行为为唯一真相，公开接口严格同名，内部实现通过规范命名映射保持一致性参考，而不是强行要求不同语言逐成员硬拷贝。推荐方案是交付“一份主文档 + 多个专题章节”的文档包，围绕系统总览、启动与装配、通信契约、模块规格、SceneGraph 与显示、UI/Shell、资源与音频、验证基线八个核心面向，逐层冻结项目事实与命名规则。

2026-05-22 实施更新：当前代码已完成基础 UI 框架简化重构，默认 UI 主链路已收缩为 `UiActionDispatcher -> ILogicRuntimePort(LogicRuntime) -> ModuleLogicHandler`，`LocalLogicGateway` 与 `AppMessageCenter` 不再代表当前实现状态。下文涉及这两个对象的段落，如未特别说明，均应视为“重构前基线分析”而不是当前代码事实。

**需求摘要**
- 已确认目标：为 VTKQtCore 编写极其详尽的项目文档，使新加入项目的开发者和跨平台、跨语言重写团队都能完整理解并复刻系统。
- 已确认目标：文档不仅解释架构，还要冻结接口名、字段名、枚举值、事件名、模块注册名、页面/功能名和关键行为。
- 已确认目标：文档粒度需覆盖模块职责与边界、输入输出报文/消息表、状态机/时序图、页面控件说明与交互流程、SceneGraph 节点变化规则、异常流与错误处理。
- 已确认目标：文档需要把关键源码入口正式纳入索引，允许读者从规范跳到关键类和函数。
- 已确认交付形式：采用“主文档 + 多个专题章节”的文档包，而不是单篇超长文档。
- 已确认真相基线：以当前仓库实际代码行为为准；已有设计文档仅作为辅助解释，不作为覆盖源码事实的上位真相。
- 已确认命名策略：公开接口严格一致；内部成员和私有实现不强制逐成员跨语言同名，但必须建立规范命名映射，保证实现者知道每个概念在目标语言中的标准对应项。
- 明确非目标：本次方案阶段不直接改业务代码，也不先写零散介绍型文档。

**调研结论**
- 仓库根目录已经长期采用中文 Markdown 沉淀架构和规范内容，当前方案文件继续放在根目录是合适的，后续正式文档也宜保持同风格。
- 当前已有文档主要覆盖整体架构、Socket 通信、UI 框架、模块间 UI 事件和音频使用，但覆盖粒度仍偏“现有开发指导”，不足以直接支撑第三方跨语言复刻。
- 当前系统主链路已可稳定描述：main.cpp 启动应用，BaseSoftwareInitializer 和具体 Initializer 负责软件装配，CommunicationHub 与 LegacySocketAdapter 负责 socket 模式兼容接入，LogicRuntime 负责中枢分发与模块直路由，ModuleLogicHandler 负责模块业务，SceneGraph 与 DisplayManager 负责三维显示。
- 当前默认装配的主业务模块可从 DefaultSoftwareInitializer 直接确认，为 datagen、params、pointpick、planning、navigation；此外还存在 intermoduletest 和 workflowshell 这类辅助/壳层相关模块，文档中应区分“主业务模块”和“调试/壳层模块”。
- contracts 层已经定义了一批跨边界契约，包括 AppMessage、UiAction、LogicNotification、ModuleInvoke、ModuleUiEvent、PromptAudioPresetIds 等；其中 AppMessage 的 target、kind、eventName、payload 等字段及 kind 的字符串映射是跨语言重写必须冻结的核心对象。
- 当前缺口集中在六类：
- 第一，协议和契约虽存在于头文件，但缺少完整、面向外部实现者的结构定义、字段语义、边界条件和序列化示例。
- 第二，模块注册名、模块能力、模块生命周期、模块切换时序和模块间依赖缺少集中、规范化说明。
- 第三，SceneGraph 节点模型、DisplayManager 刷新机制、显示层与数据层同步契约还没有形成可直接复刻的规格说明。
- 第四，软件装配、UI 工厂、页面栈、全局 UI、资源与主题加载、音频预设和启动参数等装配规则尚未系统化。
- 第五，公开命名与内部规范命名的映射关系尚未建立，后续跨语言实现容易出现语义一致但命名漂移的问题。
- 第六，缺少“如何证明跨语言实现与当前项目一致”的验收基线、案例矩阵和示例输入输出。

**推荐方案**
- 推荐方向：建设一套“主规范驱动”的项目全量对标文档包，而不是继续追加零散说明。主文档负责定义阅读入口、统一术语、模块地图、契约索引、时序总览和验收口径；专题章节负责把每个关键系统面写到可复刻粒度。
- 推荐的最终交付包结构如下：
- 第一，主文档：项目总览与索引。负责说明项目目标、目录地图、分层结构、运行主链路、模块总表、专题导航、术语表和源码索引。
- 第二，启动与装配规范。覆盖 main.cpp、SoftwareInitializerFactory、BaseSoftwareInitializer、DefaultSoftwareInitializer、ModuleUiAssemblers、PageManager、GlobalUiManager、WorkspaceShell 的职责、调用顺序、配置入口和装配约束。
- 第三，通信协议与消息契约规范。覆盖 Socket envelope、LegacySocketAdapter、AppMessage、UiAction、LogicNotification、ModuleInvoke、ModuleUiEvent、StateSample、ErrorInfo、模块注册名、固定事件名、错误码/错误消息约定和 JSON 示例。
- 第四，运行时与生命周期规范。覆盖 LogicRuntime、ILogicRuntimePort、ModuleLogicRegistry、模块激活/切换/重同步、广播/定向/全局 UI/壳层事件流转和异常流。
- 第五，模块总规格。先给模块矩阵，再为 datagen、params、pointpick、planning、navigation 分别建立独立规格章；intermoduletest 和 workflowshell 归入附录或辅助模块章。
- 第六，SceneGraph 与显示规范。覆盖节点类型、节点字段、所有权、显示目标、更新原子性、VtkSceneWindow 刷新流程、NodeDisplayManager 派生类职责和渲染一致性要求。
- 第七，UI、Shell 与页面交互规范。覆盖模块页面、页面控件职责、ModuleCoordinator/ApplicationCoordinator、页面切换、全局提示/状态栏/模块导航和页面级交互约束。
- 第八，资源、主题与音频规范。覆盖 qrc 资源、图标、样式表、主题顺序、PromptAudioPresetIds、音频文件组织、提示音触发点和命名规范。
- 第九，验证基线与回归样例。覆盖协议级样例、模块级样例、场景级样例、音频触发样例、人工核对点和跨语言实现验收清单。
- 第十，命名映射附录。对每个关键对象提供“当前仓库公开命名”和“跨语言推荐命名”的对照；公开对象必须同名，内部对象仅提供规范映射，不强制目标语言逐成员照搬。
- 这套方案适合当前仓库约束，因为现有代码和文档已经天然按“启动/通信/逻辑/显示/UI 装配”分层，只是尚未上升为对外规格；最佳路径不是推倒重写，而是把现有事实抽取为稳定文档模型，并为跨语言团队提供足够密的命名、示例和验证基线。
- 推荐把“完全一致”的要求正式分层：
- 第一层，必须完全一致：Socket 字段、AppMessage/UiAction/LogicNotification/ModuleInvoke/ModuleUiEvent 的公开字段名、枚举值、固定字符串、moduleId、公开事件名、页面/功能名。
- 第二层，应尽量一致：公开类名、公开函数名、公开状态名、章节中的概念命名。
- 第三层，仅做规范映射：私有成员、内部缓存结构、目标语言特有的资源管理机制和框架适配实现。
- 这样处理不是放宽标准，而是避免把 C++/Qt/VTK 的底层实现机制错误地硬套到其他语言，导致实现成本高而且结果反而不稳定。

**实施阶段**
1. 冻结文档边界与规范模板：按当前已确认要求，固化主文档和专题章节的目录、模板、字段表格式、时序图格式、源码索引格式和术语表格式。
2. 冻结项目词汇表和对象清单：统一 moduleId、消息类型、节点类型、关键管理器、页面术语、音频预设名、特殊目标名 global/shell/global-ui，并定义公开命名与内部命名映射规则。
3. 编写主文档：完成项目目标、分层结构、运行主链路、模块地图、专题导航、源码索引和阅读顺序，让新开发者能先建立整体认知。
4. 编写启动与装配规范：从 main.cpp、BaseSoftwareInitializer、DefaultSoftwareInitializer、ModuleUiAssemblers、WorkspaceShell 等处抽取启动参数、装配顺序、模块启用条件、默认模块、显示顺序和全局 UI 装载规则。
5. 编写通信与契约规范：逐一提炼 AppMessage、UiAction、LogicNotification、ModuleInvoke、ModuleUiEvent、StateSample、PromptAudioPresetIds 等公开对象，为每个对象补字段语义、默认值、边界条件、示例 JSON/示例交互和兼容说明。
6. 编写运行时与生命周期规范：梳理 LogicRuntime、ILogicRuntimePort、ModuleLogicRegistry、UiActionDispatcher、ApplicationCoordinator、ModuleCoordinator 之间的真实调用流；输出模块激活、模块切换、广播消息、定向消息、重同步、断连和错误处理的时序说明。
7. 编写模块总规格与逐模块规格：
8. 先产出模块矩阵，列清模块名、职责、输入、输出、依赖、页面、关键类和验证点。
9. 再对 datagen、params、pointpick、planning、navigation 分别下钻，补页面控件、交互流程、消息表、状态机、SceneGraph 变化表、异常流和示例场景。
10. 单独处理 intermoduletest 与 workflowshell，明确它们是测试/壳层能力，不与主业务模块混写。
11. 编写 SceneGraph 与显示规范：抽取节点类型、关键字段、更新规则、显示目标、DisplayManager 对应关系、VtkSceneWindow reconcile 流程和渲染一致性检查点。
12. 编写 UI、Shell、资源与音频规范：整理页面布局、WorkspaceShell 分区、主题样式、qrc 资源路径、音频预设和提示音触发点，并说明哪些内容属于公开约束，哪些仅是当前实现策略。
13. 编写验证基线与回归样例：为协议、模块、场景、音频和模块切换分别提供输入样例、预期状态变化、预期通知、预期显示效果和预期异常行为。
14. 最后统一交叉引用、章节索引、术语表、变更策略和版本维护方式，保证文档不仅能读，而且能长期作为实现依据和验收依据。

**相关文件 / 模块**
- d:/code/C++/VTKQtCore/方案设计-整体架构重构.md — 当前整体架构与未来方向，可复用为系统总览和术语基础。
- d:/code/C++/VTKQtCore/项目对标文档/06-UI与Shell页面交互规范.md — 当前 UI 分层、产品根界面装配和全局服务边界，是总览章节的核心来源之一。
- d:/code/C++/VTKQtCore/方案设计-UI去壳层化重构.md — 当前 UI 去壳层化的实施基线，是启动与装配专题的核心来源之一。
- d:/code/C++/VTKQtCore/Socket通信架构说明.md — Socket 协议、兼容层和路由过程的主要来源。
- d:/code/C++/VTKQtCore/模块间UI事件接口说明.md — 模块间 UI 事件的现有规则来源。
- d:/code/C++/VTKQtCore/声音播放使用说明.md — 音频预设与音频资源装配的现有说明来源。
- d:/code/C++/VTKQtCore/src/main.cpp — 启动参数、应用初始化、profile 与主题装配来源。
- d:/code/C++/VTKQtCore/src/contracts/AppMessage.h — 内部统一消息模型，对外规格的核心输入。
- d:/code/C++/VTKQtCore/src/contracts/UiAction.h — UI 动作契约来源。
- d:/code/C++/VTKQtCore/src/contracts/LogicNotification.h — Logic 到 UI 的通知契约来源。
- d:/code/C++/VTKQtCore/src/contracts/ModuleInvoke.h — 模块间调用契约来源。
- d:/code/C++/VTKQtCore/src/contracts/ModuleUiEvent.h — 跨模块 UI 事件字段与固定字符串来源。
- d:/code/C++/VTKQtCore/src/contracts/PromptAudioPresetIds.h — 音频预设名与固定标识来源。
- d:/code/C++/VTKQtCore/src/communication/datasource/StateSample.h — 状态样本对象来源。
- d:/code/C++/VTKQtCore/src/communication/hub/CommunicationHub.h — 通信连接、收包与健康状态来源。
- d:/code/C++/VTKQtCore/src/communication/routing/LegacySocketAdapter.h — Socket envelope 到内部消息的映射来源。
- d:/code/C++/VTKQtCore/src/logic/runtime/LogicRuntime.h — 运行时总线与模块流转的核心入口。
- d:/code/C++/VTKQtCore/src/logic/runtime/ILogicRuntimePort.h — UI 到运行时的最小抽象边界来源。
- d:/code/C++/VTKQtCore/src/logic/registry/ModuleLogicRegistry.h — 模块注册与模块能力清单来源。
- d:/code/C++/VTKQtCore/src/app/software/BaseSoftwareInitializer.h — 软件装配骨架来源。
- d:/code/C++/VTKQtCore/src/app/software/ConcreteSoftwareInitializers/DefaultSoftwareInitializer.cpp — 默认启用模块、显示顺序、初始模块和壳层装配来源。
- d:/code/C++/VTKQtCore/src/app/software/ModuleUiAssemblers.cpp — 模块 UI 工厂与页面装配来源。
- d:/code/C++/VTKQtCore/src/ui/coordination/ApplicationCoordinator.h — 全局 UI 协调和分发来源。
- d:/code/C++/VTKQtCore/src/ui/coordination/ModuleCoordinator.h — 模块页面协调来源。
- d:/code/C++/VTKQtCore/src/ui/coordination/UiActionDispatcher.h — UI 发起动作和定向发送入口来源。
- d:/code/C++/VTKQtCore/src/shell/WorkspaceShell.h — 壳层布局分区与挂载点来源。
- d:/code/C++/VTKQtCore/src/logic/scene/SceneGraph.h — 场景数据真相与节点规格来源。
- d:/code/C++/VTKQtCore/src/display/NodeDisplayManager.h — 显示层适配接口与刷新契约来源。
- d:/code/C++/VTKQtCore/src/ui/vtk3d/VtkSceneWindow.h — 场景窗口 reconcile 流程来源。
- d:/code/C++/VTKQtCore/src/modules — 各业务模块、测试模块和壳层模块的规格来源。

**验收标准**
- 文档读者无需依赖口头补充，就能说清该项目的系统分层、运行主链路、模块边界和关键对象关系。
- 文档读者能够据文档还原所有公开或半公开协议，包括消息字段名、模块注册名、关键枚举值和固定事件名。
- 文档读者能够逐模块理解输入、输出、状态变化、页面控件职责、UI 行为和场景变化，而不是只看到源码目录。
- 文档读者能够根据文档在其他语言中建立一套行为一致的通信层、逻辑层、场景层、UI 装配层和资源/音频层。
- 文档读者能够区分哪些命名必须逐项保持一致，哪些内部实现只需遵守规范映射。
- 文档中明确给出验证方法，能判断第三方实现是否与当前项目一致，而不是停留在“看起来差不多”。

**验证方式**
1. 对照主链路抽查：从启动、Socket 消息进入、模块切换、场景刷新、音频提示五条主链路逐一核对是否有完整说明。
2. 对照 contracts 抽查：检查 AppMessage、UiAction、LogicNotification、ModuleInvoke、ModuleUiEvent、PromptAudioPresetIds 是否都被纳入正式文档。
3. 对照模块抽查：随机抽 2-3 个模块，确认文档是否能解释其职责、输入输出、页面结构、行为时序、SceneGraph 变化和异常流。
4. 对照命名抽查：抽查公开接口与命名映射附录，确认“必须同名”和“规范映射”两个层次是否区分清楚。
5. 对照复刻场景抽查：任选一个其他语言实现者，只给文档不讲源码，判断其是否能准确描述需要实现的系统结构、协议、模块行为和验收方式。

**风险与缓解**
- 风险：如果目标粒度不清，文档很容易退化成“架构介绍”或“源码注释汇编”。 — 缓解：先明确读者和复刻目标，再定义每章必须回答的问题。
- 风险：如果坚持所有内容都塞进一篇文件，后续维护和交叉引用会非常差。 — 缓解：采用主文档驱动、多章节规范的组织方式。
- 风险：如果不冻结 moduleId、事件名、字段名和枚举值，跨语言重写时会出现语义相近但名字不一致的偏差。 — 缓解：把这些内容提升为正式规范并建立统一清单。
- 风险：如果要求不同语言连私有成员都逐项同名，文档会变成对某种实现语言的绑定说明，反而削弱跨语言可落地性。 — 缓解：公开对象严格同名，内部对象只做规范映射。
- 风险：部分行为只存在于实现代码，没有现成文档。 — 缓解：后续文档编写阶段按模块补做源码抽取和示例验证，不依赖现有 Markdown 是否齐全。
- 风险：如果不区分主业务模块与辅助/测试模块，读者会误把实验链路当成产品能力。 — 缓解：在主文档中显式标注模块类别和优先级。

**决策与假设**
- 已确认：仓库已有多份架构说明，可复用为主文档素材，但现状不足以支撑异构重写。
- 已确认：需要覆盖接口、变量、行为、模块边界、页面交互和 SceneGraph 变化，而不是仅做高层介绍。
- 已确认：最终交付形式是一份主规范加多个专题章节。
- 已确认：文档必须以当前仓库代码行为为唯一真相基线。
- 已确认：公开接口严格同名；内部实现采用规范命名映射，而不是强制逐语言逐成员同名。
- 当前假设：后续正式文档编写时，主业务模块的详细规格优先级高于 intermoduletest/workflowshell 等辅助模块。

**待确认项**
- 无阻塞项。后续进入正式文档编写时，只需再细化每个专题章节的文件命名和分批产出顺序。

## 补充主题：按“空白基础 UI 框架”目标简化当前架构

### 当前判断

如果目标是“一个空白基础 UI 框架，有一个 UI 管理中心注册各模块 page，可按需要初始化各种 UI；各模块 logic 根据输入消息更新节点或 UI 显示”，那么当前项目的通信与运行时链路确实偏重了。

但需要明确区分两件事：

- UI 装配层并不算复杂，甚至已经接近目标形态。
- 真正偏重的是 UI 动作进入 logic 之前的通信兼容层和消息总线层。

换句话说，当前项目不是“UI 框架设计错了”，而是“为了兼容 socket-first 运行模式、旧 envelope、多目标消息路由和全局健康状态，把一个本可以很薄的本地框架叠成了多层转发架构”。

### 为什么说 UI 层其实已经接近目标

当前代码里，以下对象组合已经基本构成了一个“UI 管理中心 + 模块注册式页面框架”：

- BaseSoftwareInitializer：负责初始化骨架、注册模块 logic 和模块 UI。
- ModuleUiAssemblers：负责按模块创建 page、VTK window、辅助面板并注册。
- ApplicationCoordinator：负责模块协调器注册、模块切换、通知转发。
- ModuleCoordinator：负责模块 page 和辅助区生命周期。
- PageManager：负责中心页面栈的 moduleId -> QWidget 映射。
- WorkspaceShell：提供 center/right/bottom/top 挂载位。

就这个目标而言，这一层不但不用推翻，反而应被提纯为未来框架的核心。

### 真正过重的部分

当前偏重主要来自以下几层叠加：

1. UiActionDispatcher -> ILogicGateway -> LocalLogicGateway -> CommunicationHub(loopback) -> LogicRuntime
2. LogicRuntime -> AppMessageCenter -> ModuleLogicRegistry -> ModuleLogicHandler
3. CommunicationHub -> MessageRouter -> LegacySocketAdapter/Runtime 兼容入口

这些层并非全部无意义，而是服务于“socket 兼容、断连重连、心跳、健康状态、全局目标域、多来源消息统一建模”这些扩展目标。

如果目标退回到“本地 UI 框架 + 模块逻辑更新节点/UI”，那当前至少有三类复杂度是可以降级的：

- LocalLogicGateway 的本地/远端双路径判断
- AppMessageCenter 的通用 AppMessage 总线建模
- CommunicationHub/MessageRouter 的 loopback 和多类别消息桥接

### 代码层面的直接证据

从当前实现看，以下判断成立：

- UiActionDispatcher 本身非常薄，它主要只是把 command 包装成 UiAction，然后调用 gateway 发送。
- LocalLogicGateway 对纯本地模式几乎只是“收到 action 后再转给 LogicRuntime”，复杂度主要来自 socket 模式下的条件转发与重同步。
- AppMessageCenter 设计支持 publish、dispatchUiIntent、dispatchStateSample、dispatchModuleEvent 以及多种 AppMessageKind，但当前真正稳定使用的核心路径仍然是 UI intent 和 state sample 路由。
- ApplicationCoordinator + ModuleCoordinator + PageManager 这组对象已经天然符合“中心化注册 page + 生命周期管理”的目标。

### 推荐的简化目标架构

如果按你要的目标收缩，推荐把未来框架稳定在 4 层：

1. UI Host Layer
	- MainWindow
	- WorkspaceShell
	- 可选的 GlobalUiManager

2. UI Registration / Coordination Layer
	- BaseSoftwareInitializer 或等价 Bootstrap
	- ApplicationCoordinator
	- ModuleCoordinator
	- PageManager
	- ModuleUiAssemblers 或等价注册器

3. Logic Runtime Layer
	- LogicRuntime
	- ModuleLogicRegistry
	- ModuleLogicHandler
	- SceneGraph

4. Optional Communication Adapter Layer
	- CommunicationHub
	- LegacySocketAdapter
	- MessageRouter
	- 任何 socket/redis/pubsub/外部协议适配层

这里的关键变化是：第 4 层从“默认主链路”降级为“可选外部适配层”，而不是所有 UI 动作都先按通信系统建模。

### 推荐保留的对象

以下对象建议保留，并作为未来简化框架的稳定核心：

- ApplicationCoordinator
- ModuleCoordinator
- PageManager
- WorkspaceShell
- BaseSoftwareInitializer
- ModuleUiAssemblers
- LogicRuntime
- ModuleLogicRegistry
- ModuleLogicHandler
- SceneGraph

原因很简单：这批对象已经直接对应你的目标，没有明显的设计方向错误。

### 推荐降级为可选层的对象

以下对象不一定要删除，但不应再占据主链路：

- CommunicationHub
- MessageRouter
- LegacySocketAdapter
- LocalLogicGateway
- AppMessageCenter 的通用 publish / module event / shell event 能力

这些对象更适合被重新定义为“当系统需要接外部消息源时，才挂接到 LogicRuntime 的适配器”，而不是所有场景下都必须经过的中心层。

### 推荐直接合并/简化的点

#### 一、UiActionDispatcher 直接面向 LogicRuntime

当前最值得简化的一步，是把：

- UiActionDispatcher -> ILogicGateway -> LocalLogicGateway -> LogicRuntime

收缩为：

- UiActionDispatcher -> LogicRuntime

这一步的价值很高，因为它并不改变模块 page 的写法，却能直接拿掉一层网关抽象和一层运行模式判断。

#### 二、把 AppMessageCenter 从“默认总线”降为“可选路由器”

对于本地框架来说，更自然的主链路应该是：

- LogicRuntime 直接根据 targetModule 或当前模块找到 ModuleLogicHandler
- handler 处理 action/state sample
- handler 修改 SceneGraph 或发出 LogicNotification

也就是说，AppMessageCenter 不需要再作为所有 action 都必须穿过的一层。它如果保留，应该只在“外部消息结构复杂、需要兼容多协议、多来源”的场景下启用。

#### 三、CommunicationHub 只做外部适配，不做本地主通道

当前 socket 模式下的 loopback 逻辑，本质是为了保证“即使服务端未回包，本地 UI 也能立刻更新”。这在兼容模式下合理，但不应该成为未来基础框架的默认路径。

基础框架应该先有本地直连主通道：

- UI -> LogicRuntime -> ModuleLogicHandler

然后只有在需要外部协议时，再增加：

- CommunicationHub -> LogicRuntime

### 推荐的最小主链路

未来如果只保留最符合目标的一条主链路，应当是：

1. page 向 UiActionDispatcher 发送命令
2. UiActionDispatcher 直接调用 LogicRuntime
3. LogicRuntime 根据 moduleId 路由到 ModuleLogicHandler
4. ModuleLogicHandler 更新 SceneGraph 或发出 LogicNotification
5. ApplicationCoordinator / ModuleCoordinator 把通知转回 page 或 shell

这个结构已经足够支撑：

- 模块页面注册
- 模块切换
- 模块内交互
- 模块间消息
- SceneGraph 更新
- 页面显示刷新

而不需要先引入完整消息总线和通信兼容体系。

### 按你的目标，建议的职责重新划分

#### UI 管理中心

建议明确由 ApplicationCoordinator 承担 UI 管理中心角色，职责固定为：

- 注册模块 coordinator
- 注册/切换 page
- 挂载模块辅助区
- 接收 LogicNotification 并路由给页面或壳层

不要再让它承担通信桥接和协议建模职责。

#### 模块 page

模块 page 只负责：

- 展示自身 UI
- 收集用户输入
- 通过 dispatcher 发消息
- 接收 notification 刷新显示

#### 模块 logic

模块 logic 只负责：

- 解释 action / state sample
- 更新 SceneGraph
- 生成通知给 UI
- 处理模块间调用

这正是你想要的“logic 根据传入消息更新节点或 UI 显示”。

#### 通信层

通信层只负责：

- 把外部消息转换为 UiAction / StateSample / 兼容命令
- 把这些消息交给 LogicRuntime

不要让通信层继续管理本地 UI 主流程。

### 推荐的简化路线

#### 第一阶段：目标收口，不改 UI 写法

- 保留现有 ApplicationCoordinator、ModuleCoordinator、PageManager、WorkspaceShell、BaseSoftwareInitializer、ModuleUiAssemblers。
- 保留 ModuleLogicHandler + SceneGraph + LogicNotification 模式。
- 把 LocalLogicGateway 定位为兼容层，而不是默认主入口。

#### 第二阶段：精简动作主链路

- 让 UiActionDispatcher 可以直接调用 LogicRuntime。
- ILogicGateway 保留为测试/mock 或远端适配接口，不再是本地主链路必经层。

#### 第三阶段：精简 Runtime 中的消息中间层

- 让 LogicRuntime 直接完成常规 action/state 路由。
- AppMessageCenter 只保留在确实需要统一外部消息模型时使用。

#### 第四阶段：把 CommunicationHub 降为插件式适配层

- 默认纯本地框架不初始化 CommunicationHub。
- 只有 socket 软件配置或特定产品变体才挂接外部通信。

### 单一推荐方案

推荐方向不是“重写整个项目”，而是“把当前已经正确的 UI 注册与模块逻辑骨架保留下来，把通信兼容层从主链路上剥离”。

原因如下：

- 你真正要的基础 UI 框架，其实已经在当前 BaseSoftwareInitializer + ApplicationCoordinator + ModuleCoordinator + PageManager 这一组对象里成形了。
- 当前复杂度主要不是来自 page 注册和 UI 初始化，而是来自 socket-first 兼容设计。
- 所以正确优化路径不是先动 UI 框架，而是先把 CommunicationHub / LocalLogicGateway / AppMessageCenter 从默认必经链路，降级为可选适配层。

### 对当前项目的最终结论

结论可以明确写成一句话：

当前项目对“基础 UI 框架”这个目标来说，通信与消息总线部分偏复杂；但 UI 注册和模块装配骨架已经接近正确形态。最优解不是推翻重做，而是保留 UI 管理中心与模块注册机制，压缩本地主链路，把通信兼容体系降为可选外部适配层。