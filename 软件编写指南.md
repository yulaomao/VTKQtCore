# 软件编写指南

## 项目定位

这是一个 Qt + VTK + socket 通信的模块化客户端工程。开发新功能时，优先沿着 UI action -> LogicRuntime -> ModuleLogicHandler -> SceneGraph -> LogicNotification 的路径工作。

## 推荐阅读顺序

1. [Socket通信架构说明.md](Socket通信架构说明.md)
2. [UI_FRAMEWORK_GUIDE.md](UI_FRAMEWORK_GUIDE.md)
3. [模块间UI事件接口说明.md](模块间UI事件接口说明.md)
4. [声音播放使用说明.md](声音播放使用说明.md)

## 新模块开发流程

1. 在 `src/modules/<module>/` 中创建模块 UI、Coordinator 和 LogicHandler。
2. 让 LogicHandler 继承 `ModuleLogicHandler`，实现 `handleAction()`，按需实现 `handleStateSample()`、`onModuleActivated()`、`onResync()`。
3. 在具体 `SoftwareInitializer` 中注册模块 logic 和模块 UI。
4. UI 只通过 `UiActionDispatcher` 发命令，不直接访问 runtime 或 SceneGraph。
5. LogicHandler 负责解释 payload、修改 SceneGraph、发出 `LogicNotification`。

## 通信入口

外部服务统一通过 socket envelope 进入客户端。`CommunicationHub` 负责连接、收包和基础健康状态，`LegacySocketAdapter` 负责兼容旧 envelope，`LogicRuntime` 负责结合 `ModuleLogicRegistry` 按模块注册名或 alias 直接路由。

推荐 envelope：

```json
{
  "module": "params",
  "type": "state",
  "value": {
    "parameters": {
      "threshold": 0.7
    }
  }
}
```

## 模块状态处理

- UI 动作进入 `handleAction()`。
- 外部状态进入 `handleStateSample()`。
- 模块间调用进入 `handleModuleInvoke()`。
- 模块激活时执行 `onModuleActivated()`。
- 全局重同步时执行 `onResync()`。

模块需要跨模块协作时优先使用 `invokeModule()` 或模块 UI 事件，不直接调用其他模块的 QWidget。

## SceneGraph 写入

- 创建节点后设置 owner、显示目标和必要属性。
- 更新点、线、模型、平面、变换节点时保持节点类型边界清晰。
- 删除节点前清理父子引用或模块持有的选中状态。
- 发出 `SceneNodesUpdated` 通知后，显示层会统一刷新。

## DataGen 与 Params 当前行为

- DataGen 使用内存态和默认种子场景；启动时不会从外部持久层恢复历史快照。
- Params 使用内存态维护参数；外部服务如需同步参数，应发送 socket state envelope。
- 重启后的历史状态恢复不属于当前框架默认能力，如需持久化应单独设计本地配置或项目级存储。

## 构建与验证

常用验证路径：

```powershell
cmake -S . -B build
cmake --build build --config Debug
```

完成模块修改后至少验证：应用能启动、模块能切换、对应 UI 动作能进入 LogicHandler、SceneGraph 更新能触发 UI 刷新、socket 状态样本能路由到目标模块。
