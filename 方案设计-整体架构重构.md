# 方案设计：整体架构重构

TL;DR：当前架构已经收敛为“外部协议兼容、运行时直路由、产品级 UI 装配”的实现。服务端仍按现有 Socket JSON envelope 发送消息，客户端在 socket 模式下通过 CommunicationHub 与 LegacySocketAdapter 做兼容接入；UI 动作主链路默认采用 UiActionDispatcher -> ILogicRuntimePort(LogicRuntime) -> ModuleLogicHandler；产品界面骨架由 C++ SoftwareInitializer 子类通过 buildProductUi 显式构建。这样既保留了现有通信兼容能力，也把后续新软件的变化面集中到 UI 布局、模块装配、主题资源和全局控件工厂。

**需求摘要**
- 已确认目标：根据传入 Socket 消息，在客户端运行时中完成统一分发，并按模块更新数据节点和 UI。
- 已确认目标：后续同类软件复用数据处理和核心流程，主要替换 UI 布局、控件、样式和模块装配，尽量减少业务逻辑改动。
- 已确认目标：简化模块间通信和全局 UI 通信，让模块页面、补充视图和全局控件都沿同一运行时边界工作。
- 已确认约束：客户端内部以当前仓库代码为准，不再把未实现的消息中心作为当前主链路描述。
- 已确认约束：新软件装配以 C++ SoftwareInitializer 子类为主，不强制配置化。
- 已确认约束：定向通信目标保持模块级，不开放任意控件级直接调用。
- 明确非目标：本次不重写服务端协议；不建议模块之间直接持有其他模块 QWidget 或控件指针；不把任意 widget 方法暴露为字符串反射接口。

**调研结论**
- 当前运行时主链路已经明确：UiActionDispatcher 通过 ILogicRuntimePort 调用 LogicRuntime，LogicRuntime 再按模块路由到 ModuleLogicHandler。
- 当前 socket 模式仍保持兼容接入：CommunicationHub 负责收发与连接状态，LegacySocketAdapter 负责旧 envelope 与内部对象之间的转换。
- 当前 UI 装配入口已经统一到 BaseSoftwareInitializer：它负责创建 GlobalUiManager、GlobalWidgetRegistry、ApplicationCoordinator，注册模块 logic、模块 UI 和全局控件工厂，然后由具体 initializer 通过 buildProductUi 返回产品根 QWidget 树。
- 当前 MainWindow 已经退化为中性根窗口：它只保留 rootStack、workspaceRootWidget、overlay/tool host 和全局控件注册表入口，不再内置固定壳层。
- 当前 ApplicationCoordinator 负责模块协调器登记、当前模块切换广播、连接状态广播和全局 UI 通知转发；ModuleCoordinator 负责主页面、补充视图和模块生命周期。
- 当前 GlobalWidgetRegistry 已成为全局类控件的唯一注册入口，按 widgetId 提供工厂登记与按需创建能力。
- contracts/AppMessage.h 仍保留统一消息模型契约，但当前代码没有 AppMessageCenter 作为运行时中心对象；当前实现的核心链路仍以 LogicRuntime 直路由为准。

**推荐方案**
- 推荐方向：继续沿“运行时直路由 + 可选通信适配 + 产品级 UI 装配”演进，而不是重新引入一个未落地的中心消息总线作为默认主链路。
- 核心分层如下：
  - Transport Adapter Layer：CommunicationHub 与 LegacySocketAdapter 负责 socket 模式的协议兼容、连接状态和 envelope 转换；本地模式下这层可以不参与 UI 动作主链路。
  - Runtime Routing Layer：LogicRuntime 作为当前唯一运行时入口，接收 UI 动作、状态样本、控制消息和连接状态变化，并按模块注册表路由给 ModuleLogicHandler。
  - Module Logic Layer：ModuleLogicHandler 负责模块业务处理、SceneGraph 更新、通知生成和必要的外发命令组织。
  - UI Coordination Layer：ApplicationCoordinator 负责全局协调与通知转发，ModuleCoordinator 负责模块页面和补充视图生命周期，UiActionDispatcher 负责把 UI 动作送入运行时。
  - Product Assembly Layer：BaseSoftwareInitializer 定义装配骨架，具体 initializer 负责启用模块、注册模块 UI、注册全局控件工厂，并通过 buildProductUi 构建产品根界面。
  - Global UI Layer：GlobalUiManager 负责当前全局通知、遮罩、确认框、错误提示和 VTK 窗口登记；GlobalWidgetRegistry 负责用户定义的全局控件工厂。
- 模块级通信模型：
  - 所有运行时定向通信统一使用模块注册名，例如 planning、navigation、params。
  - 发送方只声明目标模块、动作名和 payload，不依赖目标模块当前页面布局或控件实例。
  - 目标模块的 ModuleLogicHandler 决定消息用于更新数据、触发通知、切换流程，还是转为外发命令。
  - 不开放任意控件级目标；模块内部如何把通知分发给多个 panel/widget，属于目标模块内部实现细节。
- UI 可替换模型：
  - 数据处理、Socket 兼容、SceneGraph 更新和模块逻辑处理属于稳定核心。
  - C++ SoftwareInitializer 负责选择模块、构建产品根界面、挂载导航/状态栏/全局控件并应用主题资源。
  - 每个模块允许替换页面和补充视图，只要 ModuleCoordinator 和 UiActionDispatcher 的交互边界不变，模块逻辑无需重复实现。
  - 全局类控件通过 GlobalWidgetRegistry 按需创建，不再依赖框架预置固定实例。

**备选方案对比**
1. 保持当前架构并继续整理文档：保留 LogicRuntime、ModuleLogicHandler、ApplicationCoordinator、ModuleCoordinator 和 BaseSoftwareInitializer 主链路，只把当前实现边界文档化。优点是与仓库现状一致，风险最低；缺点是 GlobalUiManager 仍然承担多种职责，后续还可以继续拆分。
2. 在当前架构上继续做轻量演进：保持 LogicRuntime 直路由，继续增强 ModuleLogicRegistry、GlobalWidgetRegistry 和产品级装配能力，同时把全局 UI 服务拆细。优点是能继续降低 UI 变体改动面；缺点是需要持续收敛职责边界。
3. 重新引入统一 Message Center 作为默认主链路：理论上能把所有消息统一建模，但当前仓库并未按该方案落地，强行把它写成当前架构只会制造事实偏差，因此不作为当前文档基线。

**当前实现状态（2026-05-24）**
- BaseSoftwareInitializer::initialize 会依次创建 GlobalUiManager、GlobalWidgetRegistry、ApplicationCoordinator，配置 ActiveModuleState，注册模块 logic、模块 UI 与全局控件工厂，再调用 buildProductUi 生成产品根界面并挂到 MainWindow。
- MainWindow 当前提供 getWorkspaceRootWidget/setWorkspaceRootWidget、getGlobalOverlayLayer、getGlobalToolHost 和 GlobalWidgetRegistry 挂载入口，是产品界面的根容器。
- DefaultSoftwareInitializer 当前通过 DefaultProductRoot 组装顶部条、中心页栈、右侧面板和底部状态栏区域，并把 ModuleNavigationModule、ModuleStatusBarModule 和全局控件工厂创建出的控件挂进去。
- ApplicationCoordinator 当前持有应用级 UiActionDispatcher，负责广播 currentModuleChanged、connectionStateChanged、healthSnapshotChanged，并把 LogicRuntime 的 LogicNotification 转发给模块协调器和全局 UI。
- ModuleCoordinator 当前保存主页面与补充视图集合，负责 activate/deactivate 生命周期和页面通知转发。
- LogicRuntime 当前直接接收 UI 动作、控制消息、状态样本和连接状态变化，并按 ModuleLogicRegistry 把它们路由到对应的 ModuleLogicHandler。
- contracts/AppMessage.h 当前仍作为统一契约保留，但仅代表契约层对象，不代表运行时已经存在 AppMessageCenter。

**当前结构要点**
1. 启动阶段：main.cpp 创建 MainWindow、LogicRuntime、CommunicationHub 和具体 BaseSoftwareInitializer 子类；initializer 接管软件装配。
2. 装配阶段：BaseSoftwareInitializer 先注册模块 logic 和模块 UI，再注册全局控件工厂，最后通过 buildProductUi 返回产品根 QWidget 树并挂入 MainWindow。
3. UI 动作阶段：模块页面、导航条、状态栏和全局控件通过 UiActionDispatcher 把动作送到 ILogicRuntimePort；默认本地主链路直接进入 LogicRuntime。
4. socket 阶段：当 RunMode 为 Socket 时，BaseSoftwareInitializer 会把 dispatcher 与 CommunicationHub 接通，并将 CommunicationHub 的控制消息、状态样本和连接状态信号接入 LogicRuntime。
5. 运行时阶段：LogicRuntime 通过 ModuleLogicRegistry 找到对应 ModuleLogicHandler，完成业务处理、SceneGraph 更新和 LogicNotification 生成。
6. UI 刷新阶段：ApplicationCoordinator 与 ModuleCoordinator 接收 LogicNotification，刷新当前模块页面、补充视图以及全局 UI 服务状态。

**相关文件 / 模块**
- Socket通信架构说明.md — 当前 Socket envelope、CommunicationHub 与兼容路径说明。
- 项目对标文档/01-启动与装配规范.md — 当前启动装配、产品根界面和初始化流程的规范入口。
- 项目对标文档/06-UI与Shell页面交互规范.md — 当前 UI 分层、产品根界面装配和全局 UI 服务边界的规范入口。
- 模块间UI事件接口说明.md — 现有模块间 UI 事件与通知路径说明。
- src/communication/hub/CommunicationHub.h — 当前通信收发、连接状态和 socket 模式入口。
- src/communication/routing/LegacySocketAdapter.h — 当前旧 envelope 的兼容转换入口。
- src/logic/runtime/LogicRuntime.h — 当前运行时分发中心与模块路由入口。
- src/logic/runtime/ILogicRuntimePort.h — UI 到运行时的最小抽象边界。
- src/logic/registry/ModuleLogicHandler.h — 当前模块逻辑基类。
- src/logic/registry/ModuleLogicRegistry.h — 当前模块注册、descriptor 与 alias 管理入口。
- src/ui/coordination/UiActionDispatcher.h — 当前 UI 动作发送入口。
- src/ui/coordination/ApplicationCoordinator.h — 当前全局协调和通知转发入口。
- src/ui/coordination/ModuleCoordinator.h — 当前模块页面与补充视图协调入口。
- src/ui/globalui/GlobalUiManager.h — 当前全局通知、遮罩、确认框、错误提示和 VTK 窗口管理入口。
- src/ui/globalui/GlobalWidgetRegistry.h — 当前全局控件工厂注册入口。
- src/app/software/BaseSoftwareInitializer.h — 当前软件装配骨架。
- src/app/software/ConcreteSoftwareInitializers/DefaultSoftwareInitializer.cpp — 当前默认产品根界面和模块装配样例。
- src/shell/MainWindow.h — 当前应用根窗口与 workspaceRootWidget 宿主。

**验收标准**
- 现有服务端 Socket JSON envelope 不改，客户端仍能接收 module/type/value 并正确更新对应模块数据、SceneGraph 和 UI。
- UI 触发需要外发的动作时，仍能通过 UiActionDispatcher 与 CommunicationHub 的现有接线发送服务端可识别的控制消息。
- 模块间通信继续以模块注册名为边界；发送方不依赖目标 QWidget、panel 或控件实例。
- 新增一个同功能不同 UI 的软件时，只需要新增或修改 C++ SoftwareInitializer、模块页面/补充视图、样式资源和全局控件工厂，不修改核心模块逻辑。
- 任一模块 UI 替换后，只要 UiActionDispatcher、LogicNotification 和 ModuleCoordinator 的交互边界不变，该模块的数据处理、节点更新和外发命令行为保持一致。
- 全局通知、遮罩、确认框、错误提示和 VTK 窗口登记继续由统一全局服务负责，不需要业务模块直接管理这些宿主细节。
- 未注册模块、未知动作、payload 缺字段、Socket 断连等异常能形成统一错误提示或日志，而不是散落在多个 UI 页面里。

**验证方式**
1. Socket 兼容验证：使用现有测试服务发送旧格式 module/type/value 消息，确认 params、planning、navigation 等模块仍可更新数据和 UI。
2. 模块定向通信验证：从模块 A 发送动作到模块 B 的 moduleId，确认只有目标模块逻辑处理器收到并更新自身状态，其他模块不响应。
3. UI 装配验证：为一个具体 initializer 替换产品根界面布局或控件组合，确认 BaseSoftwareInitializer 仍能挂载产品根界面并进入初始模块。
4. 全局控件验证：通过 GlobalWidgetRegistry 创建全局控件，确认它可以挂载到产品根界面并通过 UiActionDispatcher 与运行时交互。
5. 回归验证：运行通信、数据读取和模块切换相关验证，确认 LogicRuntime 直路由、ModuleCoordinator 补充视图显示和全局 UI 服务行为保持正常。
6. 调试验证：检查关键链路日志，确认一条 UI 动作或 socket 输入可以定位到来源模块、目标模块、处理结果和错误信息。

**风险与缓解**
- 风险：GlobalUiManager 仍同时承担通知、遮罩、确认、错误提示和 VTK 窗口登记，职责偏重。缓解：后续可以按服务边界拆成更小的全局 UI 服务，但不改变当前对外使用方式。
- 风险：默认产品根界面目前仍采用顶部/侧边/底部的具体布局实现，产品变体过多时可能出现重复。缓解：继续把差异收敛在具体 initializer 和可复用 helper，而不是回到框架预设壳层。
- 风险：socket 模式与本地模式共存，接线点较多。缓解：保持 UiActionDispatcher -> ILogicRuntimePort 的本地主链路不变，仅在 Socket 模式下额外挂接 CommunicationHub。
- 风险：contracts/AppMessage.h 与当前运行时主链路并不等价，开发者可能误把它当成默认入口。缓解：在文档和代码注释中明确它是契约对象，而当前运行时入口仍是 LogicRuntime。

**决策与假设**
- 已确认：客户端内部以当前仓库代码行为为唯一真相。
- 已确认：服务端 Socket 协议继续兼容现有 JSON envelope。
- 已确认：新软件以 C++ SoftwareInitializer 子类作为主要装配方式。
- 已确认：定向通信目标为模块级，而不是任意控件级。
- 当前假设：SceneGraph 继续作为场景节点唯一真相，显示管理层继续负责 VTK 渲染对象生命周期。
- 当前假设：后续若继续演进全局 UI 服务或产品装配 helper，应建立在当前直路由与产品根界面模型之上，而不是回退到固定壳层或未实现消息中心。

**待确认项**
- 无阻塞项。当前文档已按仓库现状同步到可直接作为实现参考的版本。