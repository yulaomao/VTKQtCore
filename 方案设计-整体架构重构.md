# 方案设计：整体架构重构

TL;DR：本次重构建议采用“外部协议兼容、内部消息中心重构、UI 变体装配稳定化”的方案。服务端仍按现有 Socket JSON envelope 发送消息，客户端内部统一转换为模块级消息，由新的 Module Message Center 按注册名投递给模块控制器；模块 UI 只作为可替换视图，由 C++ SoftwareInitializer 负责装配。这样可以在不破坏现有数据处理能力的前提下，让后续新软件主要改 UI、布局、样式和模块装配，而不反复修改 Socket、LogicRuntime、SceneGraph 等核心链路。

**需求摘要**
- 已确认目标：根据传入 Socket 消息，在客户端处理中心统一处理后，按模块更新数据节点和 UI；模块 UI 事件在必要时可通过 Socket 向外发送。
- 已确认目标：后续同类软件复用数据处理和核心流程，主要替换 UI 布局、控件、样式和模块装配，尽量减少业务逻辑改动。
- 已确认目标：简化模块间通信和全局 UI 通信，支持按注册名称进行模块级定向发送。
- 已确认约束：客户端内部可重构为新架构，但必须兼容现有服务端 Socket 协议。
- 已确认约束：新软件装配以 C++ SoftwareInitializer 子类为主，不强制配置化。
- 已确认约束：定向通信目标保持模块级，不开放任意控件级直接调用。
- 明确非目标：本次不要求服务端协议同步重构；不建议模块之间直接持有其他模块 QWidget 或控件指针；不把任意 widget 方法暴露为字符串反射接口。

**调研结论**
- 现有架构已经有较完整的分层：CommunicationHub 接收 socket 数据，LogicRuntime 做中心分发，ModuleLogicHandler 处理模块业务，SceneGraph 承载场景数据，ModuleCoordinator/ApplicationCoordinator 刷新模块 UI 和全局 UI。
- 当前 Socket 推荐格式为 module/type/value，其中 module 已可对应模块注册名，global 可广播给所有模块。
- 现有 UiActionDispatcher 已提供 sendCommand、sendTargetedCommand/sendToTarget、sendModuleUiEvent 等发送入口，具备按目标模块发送的雏形。
- 现有模块间 UI 事件链路为 UI -> UiActionDispatcher -> LogicRuntime -> 目标模块 logic -> LogicNotification -> ModuleUiEventBinding -> widget，职责清楚但链路长、样板多，导致使用体验偏复杂。
- 现有 SoftwareInitializer 已承担启用模块、显示顺序、默认模块和 shell extension 装配职责，适合作为后续多软件 UI 变体的稳定入口。
- 复杂度主要来自三点：通信入口与 UI action 的内部模型不统一；模块注册名、业务别名、事件名缺少集中登记与校验；UI 变体和模块逻辑之间的替换边界不够硬。

**推荐方案**
- 推荐方向：建立新一代客户端内部架构，保留旧 Socket 协议适配层，对内统一为模块级消息中心。
- 核心分层如下：
  - Transport Adapter Layer：负责兼容现有 Socket JSON envelope，将 module/type/value 转换为内部 AppMessage；同时负责把需要外发的模块命令转换回旧 envelope。
  - App Message Center：客户端唯一的消息入口和路由中心，接收来自 Socket、UI、shell、global UI、定时数据源的消息，按目标注册名投递。
  - Module Runtime Registry：模块运行时注册表，登记 moduleId、显示名称、模块能力、可处理命令、可接收事件、UI 装配工厂和可选别名。
  - Module Controller：每个模块的业务入口，替代现有分散的 handleAction/handleStateSample/UI event 转发样板，统一处理外部数据、UI 意图、模块间事件和出站命令。
  - Module State Store：模块私有状态容器，保存业务状态、按钮状态、临时 UI 状态和最近一次数据快照；长期场景数据仍归 SceneGraph。
  - SceneGraph Service：继续作为 3D/场景节点的唯一业务真相，模块控制器只能通过明确接口更新节点。
  - View Adapter Layer：每个模块 UI 通过 ViewAdapter 与 Module Controller 交互，UI 只提交用户意图和渲染 ViewModel，不直接处理 Socket 数据或跨模块业务判断。
  - Shell/Global UI Service：shell 和 global UI 作为特殊目标域接入消息中心，例如 shell、global-ui，而不是散落在各模块里直接调用。
- 模块级通信模型：
  - 所有定向通信统一使用目标注册名，例如 planning、navigation、params、global-ui、shell。
  - 发送方只声明目标模块、消息类型、事件名和 payload，不关心目标模块当前 UI 布局或控件实例。
  - 目标模块的 Module Controller 决定消息用于更新数据、触发 UI ViewModel、切换流程，还是转为出站 Socket 命令。
  - 不开放任意控件级目标；模块内部可以自行把消息分发给多个 panel/widget，但这是目标模块内部实现细节。
- UI 可替换模型：
  - 数据处理、Socket 适配、SceneGraph 更新、模块间消息契约属于稳定核心。
  - C++ SoftwareInitializer 只负责选择模块、设置布局区域、创建 UI 变体、注册 shell/global UI 扩展、应用主题资源。
  - 每个模块允许多个 UI 实现，但必须实现同一个 ViewAdapter/Presenter 契约；新增 UI 变体时不复制模块 Controller 和数据处理逻辑。
  - 控件增删时，只调整对应模块 UI 和 ViewAdapter 映射；只要不新增业务命令，核心逻辑无需修改。
- 为什么优于直接“按 widget 名称调用”：
  - 保留模块生命周期边界，避免发送方依赖目标控件是否已创建。
  - 保留调试链路，所有消息都能在 Message Center 做日志、追踪、权限和错误处理。
  - 支持同一模块换不同 UI，因为外部只知道 moduleId，不知道具体 widget 名称。
  - 后续可增加模块别名或分组，但不会污染业务模块内部控件结构。

**备选方案对比**
1. 渐进优化现有架构：保留 LogicRuntime、ModuleLogicHandler、ModuleCoordinator 主链路，只增加 Module Manifest、事件名登记和发送门面。优点是成本低，风险小；缺点是现有链路较长的问题只能缓解，难以从根上统一 Socket、UI、全局 UI 和模块间事件。
2. 推荐方案，外部兼容、内部重构：保留现有服务端协议和 SceneGraph 思路，重构客户端内部消息中心、模块控制器和 UI 适配层。优点是能明显降低后续换 UI 的改动面，同时控制服务端兼容风险；缺点是需要一次较系统的客户端迁移。
3. 服务端客户端协议一起重构：重新定义 v2 Socket 协议和客户端内部架构。优点是长期协议更干净；缺点是当前不符合“必须兼容现有协议”的约束，联调成本和回归风险最高，不作为本次推荐。


**当前实现状态（2026-05-21）**
- 已落地第一阶段兼容重构：新增 `contracts/AppMessage.h`、`logic/runtime/AppMessageCenter.*` 和 `communication/routing/LegacySocketAdapter.*`。
- `LogicRuntime` 仍保留旧入口和 shell 级兼容逻辑，但普通 UI 意图、socket 状态样本会收敛到 `AppMessageCenter` 后按模块注册名投递。
- `ModuleLogicRegistry` 已扩展为运行时注册表承载点，可登记 `ModuleRuntimeDescriptor`、alias、capability、可接收/发出消息与 UI metadata。
- 未落地项仍包括完整 ViewAdapter 契约、所有模块 controller 化、shell/global-ui 服务完全目标域化；后续按本文阶段 4-8 继续迁移。

**实施阶段**
1. 建立内部消息模型与 Legacy Socket Adapter：定义 AppMessage、TargetAddress、MessageKind、CorrelationId、ErrorInfo；把现有 module/type/value envelope 映射到内部消息；保留旧协议收发兼容。
2. 建立 Module Runtime Registry：集中登记 moduleId、别名、模块能力、可接收消息、可发出消息、UI 工厂和装配元数据；SoftwareInitializer 改为向注册表声明模块组合和布局。
3. 引入 App Message Center：统一接收 Socket 消息、UI 意图、shell/global UI 事件和模块间事件；支持按 moduleId 定向、global 广播、shell/global-ui 特殊目标；内置日志、错误返回和未注册目标处理。
4. 抽象 Module Controller：将现有 ModuleLogicHandler 的 action/state/sample 处理迁移为统一 controller 入口；模块 controller 输出 StateChange、SceneGraphChange、ViewModelChange、OutboundCommand。
5. 抽象 View Adapter：每个模块 UI 通过 adapter 向 controller 提交 UiIntent，并接收 ViewModelChange；原有 widget 事件响应函数不直接构造 Socket envelope，也不直接调用其他模块 UI。
6. 重建 Shell/Global UI 接入：把全局通知、遮罩、模块切换、声音提示、状态栏等能力注册为 shell 或 global-ui 目标，由 Message Center 分发，不再散落调用。
7. 迁移现有模块：优先迁移 params、planning、navigation 等主流程模块；每迁移一个模块，同时补齐它的 moduleId、命令、事件、ViewModel 和出站命令契约。
8. 收敛旧接口：保留旧 UiActionDispatcher/ModuleUiEventBinding 的兼容包装一段时间，但内部实现转发到 Message Center；待模块迁移完成后再移除旧链路。
9. 建立新软件开发模板：提供一个标准 C++ SoftwareInitializer 样例，展示如何选择模块、指定 UI 变体、挂载 shell extension、应用主题和布局，而不修改业务 controller。

**相关文件 / 模块**
- `Socket通信架构说明.md` — 现有 Socket envelope、兼容类型和 UI 到 socket 路径说明，是 Legacy Socket Adapter 的兼容依据。
- `项目对标文档/06-UI与Shell页面交互规范.md` — 当前 UI 分层、产品根界面装配和全局 UI 服务边界依据，新架构应保留其中“UI 不写业务判断、逻辑不直接操作 QWidget、SceneGraph 是场景真相”的核心原则。
- `模块间UI事件接口说明.md` — 现有模块间 UI 事件链路依据，后续应收敛为 Message Center 的模块级事件能力。
- `src/communication/hub/CommunicationHub.h` — 现有通信收发入口，重构后应下沉为 Transport Adapter 或被 LegacySocketAdapter 包装。
- `src/communication/routing/MessageRouter.h` — 现有消息分类入口，重构后应迁移为协议适配层规则，不再承担应用级路由。
- `src/logic/runtime/LogicRuntime.h` — 现有中心分发入口，重构后职责应被 App Message Center 与 Module Controller 拆分。
- `src/logic/registry/ModuleLogicHandler.h` — 现有模块 logic 基类，重构后可演进为 Module Controller 基类或兼容适配器。
- `src/logic/registry/ModuleLogicRegistry` — 现有模块注册机制，重构后应升级为 Module Runtime Registry。
- `src/contracts/UiAction.h` — 现有 UI 动作契约，重构后应作为旧兼容输入或被 UiIntent/AppMessage 替代。
- `src/contracts/LogicNotification.h` — 现有逻辑通知契约，重构后应拆为 StateChange、ViewModelChange、DomainEvent 或保留为兼容层输出。
- `src/contracts/ModuleUiEvent.h` — 现有跨模块 UI 事件协议，重构后应并入模块级 MessageKind/EventName 契约登记。
- `src/ui/coordination/UiActionDispatcher.h` — 现有 UI 发送入口，重构后保留为轻量 facade，内部转发给 Message Center。
- `src/ui/coordination/ApplicationCoordinator.h` — 现有 shell 级协调入口，重构后应接入 shell/global-ui 目标域。
- `src/ui/coordination/ModuleCoordinator.h` — 现有模块 UI 协调入口，重构后应弱化为 ViewAdapter 宿主和生命周期管理。
- `src/app/software/BaseSoftwareInitializer.cpp` — 现有软件装配骨架，重构后继续作为 C++ UI 变体装配主入口。
- `src/app/software/ConcreteSoftwareInitializers/DefaultSoftwareInitializer.cpp` — 现有模块启用和顺序样例，后续新软件 initializer 可按此模式演进。

**验收标准**
- 现有服务端 Socket JSON envelope 不改，客户端仍能接收 module/type/value 并正确更新对应模块数据、SceneGraph 和 UI。
- UI 触发需要外发的动作时，仍能通过兼容适配层发送现有服务端可识别的 Socket 消息。
- 模块间通信可以用模块注册名定向发送；发送方不依赖目标 QWidget、panel 或控件实例。
- 新增一个同功能不同 UI 的软件时，只需要新增或修改 C++ SoftwareInitializer、模块 UI/ViewAdapter、样式资源，不修改 Socket Adapter、Message Center、Module Controller 的业务处理逻辑。
- 任一模块 UI 替换后，只要 ViewAdapter 契约不变，该模块的数据处理、节点更新、出站命令和模块间事件行为保持一致。
- shell/global UI 能作为明确目标接收消息，例如全局提示、遮罩、模块切换、声音提示，不需要业务模块直接调用全局 UI 对象。
- 未注册目标、未知消息类型、payload 缺字段、Socket 断连等异常能形成统一错误事件或日志，不再散落在多个层级里。

**验证方式**
1. Socket 兼容验证：使用现有 run_server.py 或等价测试服务发送旧格式 module/type/value 消息，确认 params、planning、navigation 等模块仍可更新数据和 UI。
2. 模块定向通信验证：从模块 A 发送事件到模块 B 的 moduleId，确认只有目标模块 controller 收到并更新自身 UI，其他模块不响应。
3. UI 变体验证：为一个现有模块替换新的页面布局或控件组合，只保留同一 ViewAdapter 契约，确认业务处理和 Socket 出站消息不变。
4. Global UI 验证：通过 Message Center 向 global-ui 或 shell 发送通知、遮罩、模块切换等事件，确认全局 UI 正确响应。
5. 回归验证：运行通信/数据读取相关测试，并补充 Message Center 路由、Legacy Socket Adapter 映射、Module Registry 注册冲突测试。
6. 调试验证：打开消息追踪日志，确认一条消息可以看到来源、目标、kind、eventName、correlationId、处理结果和错误信息。

**风险与缓解**
- 风险：一次性替换 LogicRuntime 主链路容易影响所有模块。缓解：先以兼容包装方式引入 Message Center，让旧 UiActionDispatcher 和 ModuleLogicHandler 能逐步迁移。
- 风险：新旧契约并存期间可能出现双路处理或重复通知。缓解：每个模块迁移时明确唯一入口，使用 feature flag 或模块迁移状态标记。
- 风险：模块级目标过粗，部分 UI 局部刷新似乎不够方便。缓解：模块内部可以定义私有 endpoint 或 ViewModel section，但不暴露给跨模块调用方。
- 风险：C++ 初始化类继续承担装配，后续软件数量增多时仍可能重复。缓解：在 SoftwareInitializer 内提炼通用装配 helper，但不强制引入配置化系统。
- 风险：旧 Socket 协议缺少版本号和强类型校验。缓解：Legacy Socket Adapter 做字段校验、默认值和错误上报，同时为未来 v2 协议预留 adapter 插槽。
- 风险：开发者绕过 Message Center 直接调用其他模块 UI。缓解：在编写指南中明确禁止跨模块 QWidget 指针调用，并把模块间通信示例统一改为 moduleId 定向消息。

**决策与假设**
- 已确认：客户端内部允许重构为新架构。
- 已确认：服务端 Socket 协议必须兼容现有 JSON envelope。
- 已确认：新软件以 C++ SoftwareInitializer 子类作为主要装配方式。
- 已确认：定向通信目标为模块级，而不是任意控件级。
- 架构假设：SceneGraph 继续作为场景节点唯一真相，显示管理层继续负责 VTK 渲染对象生命周期。
- 架构假设：各模块的数据处理能力可以从现有 ModuleLogicHandler 逐步迁移到 Module Controller，不要求一次性重写所有业务细节。

**待确认项**
- 无阻塞项。后续实现前可再细化第一批迁移模块顺序和 Message Center 的具体类名/API 命名。